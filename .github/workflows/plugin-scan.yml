name: Plugin Scan

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  security-events: write

jobs:
  scan:
    name: HOL plugin quality gate
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd

      - name: Scan plugin
        id: scan
        uses: hashgraph-online/ai-plugin-scanner-action@953cc49c330d889ce5b6195274b0acc2dd834b93
        with:
          plugin_dir: "."
          format: "sarif"
          output: "ai-plugin-scanner.sarif"
          profile: "public-marketplace"
          min_score: "80"
          fail_on_severity: "high"
          upload_sarif: "true"
          registry_payload_output: "ai-plugin-registry-payload.json"
