# Frappe SQL

Treat the task as Frappe-native SQL and ORM work.

- Prefer `frappe.db` and Query Builder when appropriate.
- Use raw SQL only when justified.
- Check `tab{DocType}` naming, permissions, and transaction boundaries.
